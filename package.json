{
  "name": "zerochat",
  "private": true,
  "version": "5.3.0",
  "description": "Cliente web autónomo de chat, agente y conocimiento local",
  "scripts": {
    "build:vendor": "node scripts/build-orama.mjs",
    "build": "npm run build:vendor && python3 bundle.py",
    "test": "node --test tests/test_*.js"
  },
  "dependencies": {
    "@orama/orama": "3.1.18"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "fake-indexeddb": "6.2.5"
  }
}
